# Voice

`VoiceSession` over `Utterance`. Holds pending and unconfirmed state so nothing reaches Collection below the confidence threshold. Only `domain` and `infrastructure` until a use case needs orchestrating.

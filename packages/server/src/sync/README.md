# Sync

Distribution. `CollectionTargetPort` plus one anti-corruption adapter per platform so their CSV dialects never leak into Collection. Only `domain` and `infrastructure` until a use case needs orchestrating.

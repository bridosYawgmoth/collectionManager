# Collection

The core domain. `Collection` is the consistency boundary over `CollectionEntry`. Full `domain` / `application` / `infrastructure` because this is where events, undo, and use-case orchestration earn their keep.
